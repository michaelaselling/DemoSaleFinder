package com.example.micka.demosalefinder;

import android.support.v7.app.AppCompatActivity;
import android.test.mock.MockContext;
import android.text.Editable;
import android.util.Log;
import android.widget.EditText;

import junit.framework.Assert;

import org.junit.Test;
import org.mockito.Mock;
import org.mockito.Mockito;

/**
 * Incompleted test, will not be tested.
 */

public class MemberActivityTest  {

  /* private EditText firstName;
   private MemberActivity memberActivity;

    @Test
    public void testCheckFirstNameTrue() {

        //Arrange
       // EditText firstName = Mockito.mock EditText(MainActivity.getAppContext());

        firstName = Mockito.mock(EditText.class);
        Log log = Mockito.mock(Log.class);

        firstName.setText("testFirstName");

        Mockito.when(firstName.getText().toString().length()).thenReturn(1);

        memberActivity = new MemberActivity();
        memberActivity.setFirstName(firstName);

        //Act
        boolean checkFirstName = memberActivity.checkFirstName();

        //Assert
        Assert.assertTrue(checkFirstName);

    }*/
}
